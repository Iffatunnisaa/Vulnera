<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vulnera - Cybersecurity Simulation</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: #1d2b53;
            color: #ffffff;
        }

        /* Navbar */
        .navbar {
            display: flex;
            align-items: center;
            justify-content: space-between; /* supaya logo di kiri dan button di kanan */
            padding: 3rem 10%;
        }

        .logo {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(45deg, #FFD66B, #FFFBEB);
            -webkit-background-clip: text; /* Untuk browser WebKit (Chrome, Safari) */
            background-clip: text;
            color: transparent; /* Membuat warna teks asli transparan */
            -webkit-text-fill-color: transparent; /* Untuk Safari */
        }

        .btn {
            display: inline-block;
            background: linear-gradient(to right, #FFD66B, #FFE7A0);
            color: #1d2b53;
            padding: 0.5rem 1.2rem; /* lebih kecil dari sebelumnya */
            font-size: 0.9rem;       /* perkecil teksnya */
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(255, 214, 107, 0.4);
        }

        /* FEEDBACK */
        .feedback-section {
        background-color: #1d2b53; /* warna biru gelap */
        padding: 50px 10%;
        display: flex;
        justify-content: center;
        }

        .feedback-container {
        display: flex;
        gap: 50px;
        max-width: 1200px;
        width: 100%;
        }

        .feedback-text {
        flex: 1;
        color: white;
        }

        .feedback-text h2 {
        font-size: 3rem;
        margin-bottom: 15px;
        background: linear-gradient(45deg, #FFD66B, #FFFBEB);
        -webkit-background-clip: text; /* Untuk browser WebKit (Chrome, Safari) */
        background-clip: text;
        color: transparent; /* Membuat warna teks asli transparan */
        -webkit-text-fill-color: transparent; /* Untuk Safari */
        }

        .feedback-text p {
        color: #b8c1d1;
        font-size: 1.1rem;
        }

        .feedback-form {
        flex: 1;
        max-width: 550%;
        display: flex;
        padding: 3rem 10%;
        }

        .feedback-form form {
        display: flex;
        flex-direction: column;
        gap: 15px;
        }

        .feedback-form input,
        .feedback-form textarea {
        background-color: rgba(73, 85, 121, 0.69);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 12px;
        padding: 12px;
        font-size: 1rem;
        color: #b8c1d1;
        outline: none;
        }

        .feedback-form input::placeholder,
        .feedback-form textarea::placeholder {
        color: rgba(200, 200, 200, 0.8);
        font-size: 0.9rem;
        }

        .feedback-form input:focus,
        .feedback-form textarea:focus {
        border-color: #4f7cff;
        }

        .feedback-form textarea {
        min-height: 150px;
        resize: none;
        }

        .feedback-form button {
        display: inline-block;
        background: linear-gradient(to right, #FFD66B, #FFE7A0);
        color: #1d2b53;
        padding: 0.5rem 1.2rem; /* lebih kecil dari sebelumnya */
        font-size: 0.9rem;       /* perkecil teksnya */
        border-radius: 25px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        border: none; /* ← ini hapus border hitam */
        outline: none; /* opsional, hilangkan outline saat fokus */
        }

        .feedback-form button:hover {
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(255, 214, 107, 0.4);
        }

        /* FOOTER */
        .footer {
            background-color: #1d2b53;
            color: #b8c1d1;
            padding: 3rem 10%;
            font-family: 'Poppins', sans-serif;
        }

        .footer-container {
            display: flex;
            justify-content: space-between; /* posisi kolom ke tengah */
            flex-wrap: wrap;
            gap: 4rem;
            
        }

        .footer-left,
        .footer-right {
            flex: 1 1 300px; /* biar proporsional */
            max-width: 300px;
            text-align: justify;
        }

        .footer-left h3,
        .footer-right h3 {
            margin-bottom: 1rem;
            font-size: 1.1rem;
        }

        .footer-left p,
        .footer-right p {
            line-height: 1.6;
            font-size: 0.95rem;
        }

        .footer-right i {
            margin-right: 8px;
        }

        hr {
            border: 0;
            border-top: 1px solid #b8c1d1;
            margin: 2rem 0 1rem;
        }

        .footer-bottom {
            text-align: center;
            font-size: 0.9rem;
        }
  </style>
</head>
<body>

<!-- Navbar -->
<header class="navbar">
    <div class="logo">Vulnera</div>
    <a href="#" class="btn">Keluar</a>
</header>

<!-- Contact / Feedback Section -->
<section class="feedback-section">
  <div class="feedback-container">
    
    <!-- Bagian kiri -->
    <div class="feedback-text">
      <h2>Suara<span class="highlight">kan Ide Anda</span></h2>
      <p>
        Kami siap mendengar ide dan kebutuhan Anda untuk keamanan digital yang lebih baik.
      </p>
    </div>

    <!-- Bagian kanan -->
    <div class="feedback-form">
      <form action="/send-feedback" method="POST">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" placeholder="Masukkan username Anda" required>

        <label for="message">Pesan</label>
        <textarea id="message" name="message" placeholder="Tulis pesan Anda di sini" required></textarea>

        <div class="feedback-form button">
            <button type="submit">Kirim</button>
        </div>
        
      </form>
    </div>

  </div>
</section>

<!-- Footer -->
<footer class="footer">
  <div class="footer-container">

    <!-- Kiri -->
    <div class="footer-left">
      <h3>Vulnera</h3>
      <p>
        platform simulasi serangan siber berbasis web 
        untuk pengujian dan visualisasi deteksi ancaman.
      </p>
    </div>

    <!-- Kanan -->
    <div class="footer-right">
      <h3>Contact</h3>
      <p><i class="fas fa-phone"></i> +62 813-6060-6683</p>
      <p><i class="fas fa-envelope"></i> admin@gmail.com</p>
      <p>Dinas Komunikasi, Informasi dan Statistik Kota Banda Aceh
        Jl. Tgk. Abu Lam U, No. 7 Banda Aceh (Komplek Balaikota).</p>
    </div>

  </div>

  <hr>
  <div class="footer-bottom">
    <p>© 2025, All Right Reserved.</p>
  </div>
</footer>

</body>
</html>
